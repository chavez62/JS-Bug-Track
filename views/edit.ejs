<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Bug</title>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
  />
  <style>
    .error {
      color: red;
      font-size: 0.875em;
    }
  </style>
</head>
<body class="container mt-5">
  <h1 class="my-4">Edit Bug</h1>
  <form action="/edit/<%= bug.id %>" method="POST" id="bugForm" novalidate>
    <div class="form-row">
      <div class="form-group col-md-6">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" class="form-control" value="<%= bug.title %>" required>
        <div class="invalid-feedback">Title is required.</div>
      </div>
      <div class="form-group col-md-6">
        <label for="module">Module:</label>
        <input type="text" id="module" name="module" class="form-control" value="<%= bug.module %>" required>
        <div class="invalid-feedback">Module is required.</div>
      </div>
    </div>
    <div class="form-group">
      <label for="description">Description:</label>
      <textarea id="description" name="description" class="form-control" rows="4" required><%= bug.description %></textarea>
      <div class="invalid-feedback">Description is required.</div>
    </div>
    <div class="form-group">
      <label for="reporter">Reporter Name:</label>
      <input type="text" id="reporter" name="reporter" class="form-control" value="<%= bug.reporter %>" required>
      <div class="invalid-feedback">Reporter name is required.</div>
    </div>
    <div class="form-group">
      <label for="dateReported">Date Reported:</label>
      <input type="date" id="dateReported" name="dateReported" class="form-control" value="<%= bug.dateReported %>" required>
      <div class="invalid-feedback">Date reported is required.</div>
    </div>
    <div class="form-group">
      <label for="steps">Steps to Reproduce:</label>
      <textarea id="steps" name="steps" class="form-control" rows="3" required><%= bug.steps %></textarea>
      <div class="invalid-feedback">Steps to reproduce are required.</div>
    </div>
    <div class="form-group">
      <label for="expectedBehavior">Expected Behavior:</label>
      <textarea id="expectedBehavior" name="expectedBehavior" class="form-control" rows="2" required><%= bug.expectedBehavior %></textarea>
      <div class="invalid-feedback">Expected behavior is required.</div>
    </div>
    <div class="form-group">
      <label for="actualBehavior">Actual Behavior:</label>
      <textarea id="actualBehavior" name="actualBehavior" class="form-control" rows="2" required><%= bug.actualBehavior %></textarea>
      <div class="invalid-feedback">Actual behavior is required.</div>
    </div>
    <div class="form-row">
      <div class="form-group col-md-4">
        <label for="severity">Severity:</label>
        <select id="severity" name="severity" class="form-control" required>
          <option value="">Select severity</option>
          <option value="Critical" <% if (bug.severity === 'Critical') { %>selected<% } %>>Critical</option>
          <option value="Major" <% if (bug.severity === 'Major') { %>selected<% } %>>Major</option>
          <option value="Minor" <% if (bug.severity === 'Minor') { %>selected<% } %>>Minor</option>
        </select>
        <div class="invalid-feedback">Severity is required.</div>
      </div>
      <div class="form-group col-md-4">
        <label for="status">Status:</label>
        <select id="status" name="status" class="form-control" required>
          <option value="">Select status</option>
          <option value="Pending" <% if (bug.status === 'Pending') { %>selected<% } %>>Pending</option>
          <option value="In Progress" <% if (bug.status === 'In Progress') { %>selected<% } %>>In Progress</option>
          <option value="Completed" <% if (bug.status === 'Completed') { %>selected<% } %>>Completed</option>
        </select>
        <div class="invalid-feedback">Status is required.</div>
      </div>
      <div class="form-group col-md-4">
        <label for="priority">Priority:</label>
        <select id="priority" name="priority" class="form-control" required>
          <option value="">Select priority</option>
          <option value="Low" <% if (bug.priority === 'Low') { %>selected<% } %>>Low</option>
          <option value="Medium" <% if (bug.priority === 'Medium') { %>selected<% } %>>Medium</option>
          <option value="High" <% if (bug.priority === 'High') { %>selected<% } %>>High</option>
        </select>
        <div class="invalid-feedback">Priority is required.</div>
      </div>
    </div>
    <button type="submit" class="btn btn-primary">Update</button>
  </form>

  <script>
    // Bootstrap form validation
    (function () {
      'use strict';
      window.addEventListener(
        'load',
        function () {
          // Fetch all the forms we want to apply custom Bootstrap validation styles to
          var forms = document.getElementsByClassName('needs-validation');
          // Loop over them and prevent submission
          var validation = Array.prototype.filter.call(forms, function (form) {
            form.addEventListener(
              'submit',
              function (event) {
                if (form.checkValidity() === false) {
                  event.preventDefault();
                  event.stopPropagation();
                }
                form.classList.add('was-validated');
              },
              false
            );
          });
        },
        false
      );
    })();
  </script>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
